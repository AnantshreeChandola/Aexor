"""
PreviewOrchestrator — Read-only plan preview with dry-run execution.

Orchestration Layer library component (no HTTP routes, no owned DB tables).
Executes a plan's previewable steps in read-only mode and returns
structured preview results for user review before execution.

Spec: specs/025-previeworchestrator-read-only/spec.md
"""
